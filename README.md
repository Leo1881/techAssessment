# Email Template Builder

A React email template builder — compose email layouts from reusable content blocks (Header, Text, Image, Button).

## Getting Started

```bash
npm install
npm run dev
npm run storybook
```
